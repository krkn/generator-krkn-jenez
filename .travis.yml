language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-krkn-jenez' ]; then cd .. && eval "mv $currentfolder generator-krkn-jenez" && cd generator-krkn-jenez; fi

